# # CUDA 编译配置
# build --cxxopt=-std=c++17
# build --host_cxxopt=-std=c++17

# # CUDA 架构设置（根据您的 GPU 调整）
# build --cuda_archs=sm_75,sm_80,sm_86

# # 优化设置
# build:opt --compilation_mode=opt
# build:opt --copt=-O3

# # 调试设置
# build:dbg --compilation_mode=dbg
# build:dbg --copt=-g

# # GPU 设备设置
# build --action_env=CUDA_VISIBLE_DEVICES=0

# 在 .bazelrc 中
build --action_env=CUDA_HOME=/usr/local/cuda
build --action_env=LD_LIBRARY_PATH=/usr/local/cuda/lib64
# build --experimental_repository_resolved_file=
# build --experimental_check_external_repository_files=false